# Aliases
alias db="(z tools/dev-env && docker-compose up -d)"
alias dbdev="(z tools/aws-rds && ./aws-rds.sh -u skim -h attentive-devel -x)"
alias dbprod="(z tools/aws-rds && ./aws-rds.sh -u skim -h attentive-prod -x)"
alias dbrep="(z tools/aws-rds && ./aws-rds.sh -u skim -h attentive-prod-replica -x)"

# MYSQL config
export PATH="/usr/local/opt/mysql-client/bin:$PATH"
export LDFLAGS="-L/usr/local/opt/mysql-client/lib"
export CPPFLAGS="-I/usr/local/opt/mysql-client/include"

export AWS_PROFILE=prod
export AWS_REGION=us-east-1
export AWS_SDK_LOAD_CONFIG=1
export E2E_USER_EMAIL=jreed@cypresstest.com
export E2E_USER_PASSWORD=NTM3NTg3MDMxMzQzNjEwODc1Nzk5MzAy
